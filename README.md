# Support

A lightweight inbox for keeping customer conversations moving.

## Development

Requires Node.js 24 or newer.

```bash
npm ci
npm run dev
```

Open [http://localhost:3000](http://localhost:3000).

## Checks

```bash
npm run check
```

The production build is a static export written to `out/`.
